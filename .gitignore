# Dependencies
node_modules/

# Build output
dist/
public/

# Wrangler local state
.wrangler/

# Env / secrets (all real config lives in Cloudflare Pages env vars)
.env
.env.*
!.env.example
.dev.vars

# OS / editor
.DS_Store
*.log

# Claude Code worktrees (nested git repos, must never be tracked as gitlinks)
.claude/worktrees/
