// Placeholder: replace with the designed UI's script (see BUILD_SPEC.md).
// Contract: fetch('/api/asana/<endpoint>'); on 401 redirect to /auth/login;
// "Log out" links to /auth/logout. No tokens in front-end code.
